# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
    transformator.token: "ece59a72-9637-4205-80de-fb2b5d39a275"
    data.msmt: "http://stistko.uiv.cz/rejstrik/rejstrik_151103.zip"

    # elastic credentials
    elastic.address: "http://134.168.35.125:9200/"
    elastic.index: "nase-skoly"
    elastic.type: "school"

    # Google Maps API
    googleMaps.key: "AIzaSyB0_V71MYMxE-uatgqNMBqANs5ZT8uH2FI"

services:
#    service_name:
#        class: AppBundle\Directory\ClassName
#        arguments: ["@another_service_name", "plain_value", "%parameter_name%"]
    app.form.registration:
        class: AppBundle\Form\RegistrationType
        tags:
            - { name: form.type, alias: app_user_registration }
    app.listener.registration:
        class: AppBundle\EventListener\RegistrationListener
        arguments: [@router]
        tags:
            - { name: kernel.event_subscriber }
    transformator.listener.token_listener:
        class: TransformatorBundle\EventListener\TokenListener
        arguments: ["%transformator.token%"]
        tags:
            - { name: kernel.event_listener, event: kernel.controller, method: onKernelController }
    transformator.utils.file_operations:
        class: TransformatorBundle\Utils\FileOperations
        arguments: ["@security.token_storage", "@doctrine.orm.entity_manager", "%data.msmt%"]

    transformator.utils.build:
        class: TransformatorBundle\Utils\Build
        arguments: ["@security.token_storage", "@ci.restclient", "@doctrine.orm.entity_manager", "%elastic.address%", "%elastic.index%", "%elastic.type%", "%googleMaps.key%"]